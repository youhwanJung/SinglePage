import 'package:flutter/material.dart';

class ContentThirdSectionWidget extends StatelessWidget {
  const ContentThirdSectionWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.deepPurple.withOpacity(0.3),
    );
  }
}
